apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: llm-router
  namespace: agentgateway-system
spec:
  parentRefs:
    - name: agentgateway-external
  rules:
    # 1. Simple matching tasks (job_match) -> cheep model
    - matches:
        - headers:
            - name: x-gateway-task-name
              value: "job_match"
      backendRefs:
        - group: agentgateway.dev
          kind: AgentgatewayBackend
          name: gemini-backend
          port: 443
    # 2. Complex tasks or by default -> expensive model
    - backendRefs:
        - group: agentgateway.dev
          kind: AgentgatewayBackend
          name: claude-backend
          port: 443
