apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: external-secrets
  namespace: flux-system
spec:
  interval: 30m
  chart:
    spec:
      chart: external-secrets
      version: "0.*"
      sourceRef:
        kind: HelmRepository
        name: external-secrets
        namespace: flux-system

  targetNamespace: external-secrets

  install:
    crds: Create
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 3
  values:
    installCRDs: true
    # Required for GCP Workload Identity on GKE (Option A below).
    # ESO's controller ServiceAccount is annotated so GKE can impersonate
    # the GCP Service Account without storing any key JSON in Kubernetes.
    serviceAccount:
      annotations:
        iam.gke.io/gcp-service-account: "eso-sa@happy-deploys-job-searcher.iam.gserviceaccount.com"